# ----------------------------------------------------------------------
# 1. Java & Compiled Files (기본 자바 컴파일 파일)
# ----------------------------------------------------------------------
*.class
*.log
*.jar
*.war
*.ear
hs_err_pid*

# ----------------------------------------------------------------------
# 2. Eclipse IDE Specific (이클립스 설정 파일 - 가장 중요!)
# ----------------------------------------------------------------------
.metadata
bin/
tmp/
*.tmp
*.bak
*.swp
*~.nib
local.properties
.settings/
.loadpath
.recommenders

# .classpath와 .project는 팀 프로젝트 정책에 따라 공유하기도 하지만,
# Maven/Gradle 기반 프로젝트라면 각자 환경에서 생성하는 것이 좋으므로 무시하는 것을 추천합니다.
.classpath
.project
.springBeans
.factorypath

# ----------------------------------------------------------------------
# 3. Build Tools (Maven & Gradle 빌드 결과물)
# ----------------------------------------------------------------------
# Maven
target/
mvnw
mvnw.cmd
.mvn/
dependency-reduced-pom.xml

# Gradle (혹시 사용할 경우를 대비해 추가)
.gradle
build/
!gradle/wrapper/gradle-wrapper.jar

# ----------------------------------------------------------------------
# 4. Spring Boot & Logs (로그 및 스프링부트 임시파일)
# ----------------------------------------------------------------------
logs/
log/
*.log
!src/main/resources/log4j2.properties 
# (설정 파일은 올려야 하므로 제외하지 않음, 단 실제 로그 파일만 무시)

# ----------------------------------------------------------------------
# 5. OS Specific (운영체제 시스템 파일)
# ----------------------------------------------------------------------
# Mac
.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# ----------------------------------------------------------------------
# 6. Web Specific (JSP/Web 개발 시 업로드 폴더 등)
# ----------------------------------------------------------------------
# 만약 파일 업로드 기능을 테스트한다면, 업로드된 파일이 저장되는 폴더는 제외해야 함
# 예: src/main/webapp/upload/
upload/
uploads/